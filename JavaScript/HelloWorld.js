var msg = 'Hello World';
console.log(msg);

//This runs through port 8080. To run, type in the URL "http://localhost:8080/"
/*
var http = require('http');
var dt = require('./DateTimeModule');

http.createServer(function (req, res) {
  res.writeHead(200, {'Content-Type': 'text/html'});
  res.write("The date and time are currently: " + dt.dateTime());
  res.end();
}).listen(8080);
*/